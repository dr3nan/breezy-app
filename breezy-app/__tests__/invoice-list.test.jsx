/* import FetchInvoiceList from '../../breezy-app/app/members-only/';
import "@testing-library/jest-dom";
import { fireEvent, render, screen } from "@testing-library/react";

describe("InvoiceList", () => {
  it("Renders all the invoices correctly", () => {
    render(<FetchInvoiceList/>);
    // check if all components are rendered
    expect(screen.getByTestId("displayInvoiceList")).toBeInTheDocument();

  });
}); */